# Easy Exercises - these exercises are really beginner level
# and you should be able to complete them with one or two lines of
# code.
# Load the walmart_data and walmart_features files
walmart_data = read.csv('./data/walmart_data.csv')
walmart_features = read.csv('./data/walmart_features.csv')
# Store the number of rows of walmart_data in a variable called
# n_rows
n_rows = nrow(walmart_data)
# Print the first 5 rows of the dataframe walmart_data
head(walmart_data, 5)
# Count the number of rows per store using the Table
# command and store the resulting object in R with the name
# rows_per_store
rows_per_store = table(walmart_data$Store)
# Convert rows_per_store to a data frame - Rewrite
# the object
rows_per_store = as.data.frame(rows_per_store)
# Medium Level Exercises
# Which store has the most rows? Retrieve this value using R code
rows_per_store[order(-rows_per_store$Freq),][1,]
# Sum the sales by store on walmart_data
# and store the data in a sum_by_store object
sum_by_store = aggregate(
walmart_data$Weekly_Sales,
by = list(walmart_data$Store),
FUN=sum
)
# rename the columns of the data frame produced
# above as store_number and total_sales
colnames(sum_by_store) <- c('store_number','total_sales')
# Plot bar plot using base r with sorting total sales
# from sales with most sales to stores least sales
# Give a title to the plot "Sales By Store"
# and color the bars darkgreen
# ignore the x labels for now
barplot(sum_by_store[order(-sum_by_store$total_sales),]$total_sales,
main = "Sales by Store",
col = "darkgreen")
# Compute the mean of every column in walmart_features
sapply(walmart_features, mean)
# Create a new column in walmart features called
# standardized_cpi subtracting the mean and dividing by the
# standard deviation
# Note: CPI has NAS!
walmart_features$sd_cpi = (
(walmart_features$CPI - mean(walmart_features$CPI, na.rm=TRUE))/
sd(walmart_features$CPI, na.rm=TRUE)
)
# Produce a line plot for sales of store number 1 for every department
# Add labels to x and y
# Hint: Check the function lines
store_1 = walmart_data[walmart_data$Store==1,]
store_1_total = aggregate(
store_1$Weekly_Sales,
list(store_1$Date),
FUN=sum
)
plot(store_1_total$Group.1,
store_1_total$x,
xlab='Weekly Sales',
ylab='Date')
store_1_total$Group.1
plot(as.Date(store_1_total$Group.1),
store_1_total$x,
xlab='Weekly Sales',
ylab='Date')
read.csv
help(read.csv)
plot(factor(store_1_total$Group.1),
store_1_total$x,
xlab='Weekly Sales',
ylab='Date')
?factor
?as.factor
factor(altitude, order = TRUE,
levels = c("Low", "Medium", "High"))
factor(altitude, order = TRUE,
levels = c("Low", "Medium", "Haltitude <- c("High", "Low", "High", "Low", "Medium","Low", "Medium")
altitude <- c("High", "Low", "High", "Low", "Medium","Low", "Medium")
# Give levels to it and let R now that is an
# ordinal vector
altitude_vector <- factor(altitude, order = TRUE,
levels = c("Low", "Medium", "High"))
altitude_vector
altitude <- c("High", "Low", "High", "Low", "Medium","Low", "Medium")
# Give levels to it and let R now that is an
# ordinal vector
altitude_vector <- factor(altitude, ordered = TRUE,
levels = c("Low", "Medium", "High"))
altitude_vector
factor(c("low", "medium", "low", "high"), order = c("low", "medium", "high"))
factor(c("low", "medium", "low", "high"), order = c("low", "medium", "high"))
factor(c("low", "medium", "low", "high"), ordered = c("low", "medium", "high"))
factor(c("low", "medium", "low", "high"), order = c("low", "medium", "high"))
factor(c("low", "medium", "low", "high"), ordered=TRUE)
factor(c("low", "medium", "low", "high"), order = c("low", "medium", "high"))
factor(c("low", "medium", "low", "high"), order = c("low", "medium", "high"))
as.factor(c("low", "medium", "low", "high"), order = c("low", "medium", "high"))
?as.factor
help(as.factor)
factor(c("low", "medium", "high"), order = c("low", "medium", "high"))
factor(c("low", "medium", "high"), order = c("low", "medium", "high"))
factor(c("low", "medium", "high"), order = c("low", "medium", "high"))
factor(c("low", "medium", "high"), order = c("low", "medium", "high"))
factor(c("low", "medium", "high"), order = c("low", "medium", "high"))
factor(c("low", "medium", "high"), ordered = c("low", "medium", "high"))
# Load the walmart_data and walmart_features files
walmart_data = read.csv('./data/walmart_data.csv')
walmart_features = read.csv('./data/walmart_features.csv')
n_rows = nrow(walmart_data)
head(walmart_data, 5)
rows_per_store = table(walmart_data$Store)
rows_per_store = as.data.frame(rows_per_store)
rows_per_store[order(-rows_per_store$Freq),][1,]
sum_by_store = aggregate(
walmart_data$Weekly_Sales,
by = list(walmart_data$Store),
FUN=sum
)
colnames(sum_by_store) <- c('store_number','total_sales')
barplot(sum_by_store[order(-sum_by_store$total_sales),]$total_sales,
main = "Sales by Store",
col = "darkgreen")
sapply(walmart_features, mean)
walmart_features$sd_cpi = (
(walmart_features$CPI - mean(walmart_features$CPI, na.rm=TRUE))/
sd(walmart_features$CPI, na.rm=TRUE)
)
store_1 = walmart_data[walmart_data$Store==1,]
store_1_total = aggregate(
store_1$Weekly_Sales,
list(store_1$Date),
FUN=sum
)
plot(store_1_total$Group.1,
store_1_total$x,
xlab='Weekly Sales',
ylab='Date')
plot(as.factor(store_1_total$Group.1),
store_1_total$x,
xlab='Weekly Sales',
ylab='Date')
plot(as.factor(store_1_total$Group.1),
store_1_total$x,
xlab='Weekly Sales',
ylab='Date')
lines(store_1_total$Group.1,
store_1_total$x)
plot(as.factor(store_1_total$Group.1),
store_1_total$x,
xlab='Weekly Sales',
ylab='Date')
lines(as.factor(store_1_total$Group.1),
store_1_total$x)
store_20 = walmart_data[walmart_data$Store==20,]
store_20_sales = aggregate(
store_20$Weekly_Sales,
list(store_20$Date),
FUN=sum
)
library(ggplot2)
(
ggplot(store_20_sales,
aes(x=as.Date(store_20_sales$Group.1), y=store_20_sales$x, group=1))
+ geom_line(color='orange') + geom_point(color='darkorange')
+ xlab('Date') + ylab('Sales')
)
# Lets do a fresh example now, the above one was just a practice.
data(mtcars)
# Get an external object to work on
car_definition <- rownames(mtcars)
# Extract the brands
brands <- sapply(
X = car_definition,
FUN = function(current_car_definition) {
definitives <- strsplit(x = current_car_definition, split = " ")
definitives[[1]][1]
}
)
# Extract the models
models <- sapply(
X = car_definition,
FUN = function(current_car_definition) {
definitives <- strsplit(x = current_car_definition, split = " ")
definitives[[1]][-1]
}
)
# This is a good practice.
brands <- as.factor(brands)
models <- as.factor(models)
brands
models
# Lets do a fresh example now, the above one was just a practice.
data(mtcars)
# Get an external object to work on
car_definition <- rownames(mtcars)
# Extract the brands
brands <- sapply(
X = car_definition,
FUN = function(current_car_definition) {
strsplit(x = current_car_definition, split = " ")[[1]][1]
}
)
# Extract the models
models <- sapply(
X = car_definition,
FUN = function(current_car_definition) {
strsplit(x = current_car_definition, split = " ")[[1]][-1]
}
)
# This is a good practice.
brands <- as.factor(brands)
models <- as.factor(models)
models <- sapply(
X = car_definition,
FUN = function(current_car_definition) {
strsplit(x = current_car_definition, split = " ")[[1]][-1]
}
)
models <- as.factor(models)
models
as.factor(models)
models
models <- sapply(
X = car_definition,
FUN = function(current_car_definition) {
strsplit(x = current_car_definition,
split = " ")[[1]][-1]
}
)
models
walmart_data = read.csv('./data/walmart_data.csv')
walmart_features = read.csv('./data/walmart_data.csv')
str(walmart_data)
head(walmart_data)
n_rows = nrow(walmart_data)
head(walmart_data, 5)
table(walmart_data$Store)
rows_per_store = table(walmart_data$Store)
rows_per_store = as.data.frame(rows_per_store)
rows_per_store
rows_per_store[order(rows_per_store$Freq),]
rows_per_store[order(-rows_per_store$Freq),'Var1']
rows_per_store[order(-rows_per_store$Freq),'Var1'[1]]
as.character(rows_per_store[order(-rows_per_store$Freq),'Var1'[1]])
as.character(rows_per_store[order(-rows_per_store$Freq),'Var1'[1]])
aggregate(
x=walmart_data$Weekly_Sales,
by=list(walmart_data$Store),
FUN = sum
)
sum_by_store = aggregate(
x=walmart_data$Weekly_Sales,
by=list(walmart_data$Store),
FUN = sum
)
sum_by_store
colnames(sum_by_store) = c('store_number','total_sales')
colnames(sum_by_store)
sum_by_store
sum_by_store[order(sum_by_store$total_sales),]
barplot(sum_by_store[order(sum_by_store$total_sales),])
barplot(sum_by_store[order(sum_by_store$total_sales),],'total_sales')
barplot(sum_by_store[order(sum_by_store$total_sales),'total_sal_sales'])
barplot(sum_by_store[order(-sum_by_store$total_sales),'total_sales'])
barplot(sum_by_store[order(-sum_by_store$total_sales),'totaltal_sales'],
main='Sales by Store',
col='red')
barplot(sum_by_store[order(-sum_by_store$total_sales),'total_sales'],
main='Sales by Store',
col='red')
sapply(X=walmart_features, FUN=mean)
sapply(X=walmart_features, FUN=mean, na.rm=TRUE)
walmart_features$CPI
mean(walmart_features$CPI, na.rm=TRUE)
walmart_features$CPI - mean(walmart_features$CPI, na.rm=TRUE)
walmart_features$CPI - mean(walmart_features$CPI, na.rm=TRUE)
(walmart_features$CPI - mean(walmart_features$CPI, na.rm=TRUE))
(walmart_features$CPI - mean(walmart_features$CPI, na.rm=TRUE))/sd(walmart_features$CPI, na.rm=TRUE)
walmart_features['standardized_cpi'] = (
(walmart_features$CPI - mean(walmart_features$CPI, na.rm=TRUE))
/
sd(walmart_features$CPI, na.rm=TRUE)
)
walmart_features['standardized_cpi'] = ( 
  walmart_features$CPI - mean(walmart_features$CPI, na.rm=TRUE) 
) / sd(walmart_features$CPI, na.rm=TRUE)
q()
walmart_features['standardized_cpi'] = (
(walmart_features$CPI - mean(walmart_features$CPI, na.rm=TRUE))
/
sd(walmart_features$CPI, na.rm=TRUE)
)
class(walmart_features$CPI)
walmart_features$CPI <- as.numeric(walmart_features$CPI)
walmart_features$CPI <- as.numeric(walmart_features$CPI)
sum(is.na(walmart_features$CPI))
walmart_features$CPI[!is.na(walmart_features$CPI) & !is.numeric(walmart_features$CPI)]
mean(walmart_features$CPI, na.rm = TRUE)
class(walmart_features$CPI)
colnames(walmart_features
)
colnames(walmart_features)
"CPI" %in% colname(walmart_features)
walmart_features
standardized_cpi
store_1 = walmart_data[walmart_data$Store==1,]
view(store_1)
store_1
aggregate(
x=stroe_1$Weekly_Sales,
by=list(store_1$Date),
FUN=sum
)
aggregate(
x=store_1$Weekly_Sales,
by=list(store_1$Date),
FUN=sum
)
plot(
x=store_1_total$Group.1,
y=store_1_total$x
)
plot(
)
store_1_total = aggregate(
x=store_1$Weekly_Sales,
by=list(store_1$Date),
FUN=sum
)
plot(
l
l
plot(
x=store_1_total$Group.1,
y=store_1_total$x,
xlab='Date',
ylab='Sales'
)
store_20 = walmart_data[walmart_data$Store==20,]
store_20
store_1_total = aggregate(
)
store_20_total = aggregate(
x=store_20$Weekly_Sales,
by=list(store_20$Date),
FUN=sum
)
store_20_total
library(ggplot2)
q()
q()
